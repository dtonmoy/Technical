# Initialize your local
cd path/to/the/project

git init
git add .
git commit -m "Initial commit"

# Set the remote origin
git remote add origin https://github.com/dtonmoy/reponame.git

git branch -M main
git push -u origin main
